{
  "name": "mayur-devhub",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/backend\" \"npm run dev --prefix apps/frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
